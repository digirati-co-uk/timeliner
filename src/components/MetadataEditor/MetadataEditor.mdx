---
title: Metadata Editor
menu: Components
---
import { Playground, Props } from 'docz';
import MetadataEditor from './MetadataEditor';

# Metadata Editor
Allows for the editing of metadata, from either a range or a manifest.

<Props of={MetadataEditor} />

## Metadata Editor Demo
<Playground>
  <MetadataEditor 
    onSave={
      result => {
        alert(JSON.stringify(result))
      }
    }
    onDelete={
      ()=>{ 
        alert('deleted');
      }
    }
  />
</Playground>
