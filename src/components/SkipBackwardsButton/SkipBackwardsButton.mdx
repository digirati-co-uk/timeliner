---
name: Skip Backwards Button
menu: Components
---

import { Playground, PropsTable } from 'docz';
import SkipBackwardsButton from './SkipBackwardsButton';

# Skip Backwards Button Component
<PropsTable of={SkipBackwardsButton}/>

## Skip Backwards Button Default State
<Playground>
    <SkipBackwardsButton onClick={()=>{alert('Play button clicked.')}} />
</Playground>

## Skip Backwards Button Disabled State
<Playground>
    <SkipBackwardsButton disabled={true} onClick={()=>{alert('This shouldn\'t render.')}} />
</Playground>
