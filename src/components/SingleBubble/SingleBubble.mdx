---
title: Single bubble
menu: Components
---

import { Playground, PropsTable } from 'docz'; 
import SingleBubble from './SingleBubble';

# Single bubble
SVG React component that will render a single bubble shape when given a height, width and style.

<PropsTable of={SingleBubble} />


<Playground>
  <svg width="200" height="50" viewBox="0 0 200 50">
    <g 
      style={{
        transform: 'scale(1, -1)',
        transformOrigin: '50% 50%',
      }}
    >
      <rect 
        width="200" 
        height="50" 
        style={{
          fill: '#f6f6f6', 
          strokeWidth: 0,
          stroke:'rgb(0,0,0,0)'
        }}
      />
      <SingleBubble 
        x={0} 
        width={50} 
        height={30} 
        colour="#ff3311"
        label="asd"
      />
    </g>
  </svg>
</Playground>