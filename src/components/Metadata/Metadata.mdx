---
title: Project Metadata
menu: Components
---

import { Playground, PropsTable } from 'docz';
import Metadata from './Metadata';
import DEMO_DATA from './Metadata.Demo';


# Project Metadata
Displays all of the metadata for a given at the current timestamp for both the manifest
and also the ranges.

<PropsTable of={Metadata} />

## Project Metadata Demo

The first section ends at 40s try to modify the currentTime to 45s.

<Playground>
  <Metadata
    ranges={DEMO_DATA}
    currentTime={30 * 1000}
    runtime={60*1000}
  />
</Playground>