---
name: Audio Transport Bar
menu: Components
---

import { Playground, Props } from 'docz';
import Component from "@reach/component-component";
import AudioTransportBar from './AudioTransportBar';

# Audio Transport Bar component
<Props of={AudioTransportBar}/>

<Playground>
  <Component initialState={{ volume: 70, currentTime: 30000, runTime: 120000 }}>
  {({ setState, state }) => (
    <AudioTransportBar 
      isPlaying={false}
      onVolumeChanged={volume => {
        setState({ volume });
      }}
      {...state}
    />
  )}
  </Component>
</Playground>

<Playground>
  <Component initialState={{ volume: 70, currentTime: 30000, runTime: 120000  }}>
  {({ setState, state }) => (
    <AudioTransportBar 
      isPlaying={true}
      onVolumeChanged={volume => {
        setState({volume});
      }}
      {...state}
    />
  )}
  </Component>
</Playground>
