---
title: Time Metadata
menu: Components
---

import { Playground, PropsTable } from 'docz';
import TimeMetadata from './TimeMetadata';
import DEMO_DATA from './TimeMetadata.Demo';


# Time Metadata
Displays all of the metadata for a given at the current timestamp for both the manifest
and also the ranges.

<PropsTable of={TimeMetadata} />

## Time Metadata Demo

The first section ends at 40s try to modify the currentTime to 45s.

<Playground>
  <TimeMetadata
    ranges={DEMO_DATA}
    currentTime={30 * 1000}
    runtime={60*1000}
    manifestLabel="Manifest label"
    manifestSummary="Manifest Summary"
  />
</Playground>